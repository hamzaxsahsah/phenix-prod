[Unit]
Description=PheonixCRM Docker Compose
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/pheonixCRM
RemainAfterExit=yes
ExecStart=/usr/bin/docker compose -f /opt/pheonixCRM/docker-compose.prod.yml up -d --build
ExecStop=/usr/bin/docker compose -f /opt/pheonixCRM/docker-compose.prod.yml down
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target